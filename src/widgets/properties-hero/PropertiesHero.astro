<section class="section-padding pt-10 md:pt-14">
  <div class="container">
    <div class="relative overflow-hidden rounded-[2rem] border border-[var(--color-border)] p-8 md:p-12">
      <div
        class="pointer-events-none absolute inset-0"
        style={`background:
          radial-gradient(560px 220px at 0% 0%, color-mix(in srgb, var(--color-primary) 22%, transparent), transparent 70%),
          radial-gradient(420px 220px at 100% 0%, color-mix(in srgb, var(--color-accent) 38%, transparent), transparent 72%),
          linear-gradient(140deg, color-mix(in srgb, var(--color-card) 85%, transparent), color-mix(in srgb, var(--color-background) 75%, transparent));`}
      >
      </div>
      <div class="pointer-events-none absolute inset-0 opacity-40 [mask-image:linear-gradient(to_bottom,black,transparent)]">
        <div class="h-full w-full bg-[linear-gradient(to_right,var(--color-border)_1px,transparent_1px),linear-gradient(to_bottom,var(--color-border)_1px,transparent_1px)] bg-[size:32px_32px]">
        </div>
      </div>
      <div class="relative">
        <p class="text-xs tracking-[0.18em] text-[var(--color-muted-foreground)] uppercase">
          curated premium portfolio
        </p>
        <h1
          class="mt-3 max-w-3xl text-[length:var(--text-h1)] leading-tight font-semibold text-balance"
        >
          Discover Premium Properties
        </h1>
        <p class="mt-4 max-w-2xl text-sm text-[var(--color-muted-foreground)] md:text-base">
          Handpicked residences in landmark locations with elevated architecture, quiet luxury and
          long-term value.
        </p>
      </div>
    </div>
  </div>
</section>
